<!DOCTYPE html>
<html lang="<%= I18n.locale %>" class="h-full bg-gradient-to-br from-blue-50 via-white to-purple-50">
<head>
  <title><%= yield(:title).presence || "ReZum - AI-Powered Resume & Cover Letter Generator" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="<%= yield(:description).presence || "Generate ATS-optimized resumes and personalized cover letters in 30 seconds. Used by professionals worldwide." %>">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Favicon -->
  <link rel="icon" href="/favicon.ico">
  
  <style>
    body { font-family: 'Inter', sans-serif; }
    .auth-container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 48px 16px;
    }
    .floating-animation {
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
    }
    .fade-in-up {
      animation: fadeInUp 0.8s ease-out;
    }
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .glassmorphism {
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.18);
    }
  </style>
</head>

<body class="h-full">
  <div class="auth-container">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <!-- Logo Section -->
      <div class="text-center fade-in-up">
        <%= link_to "/", class: "inline-flex items-center space-x-3 group" do %>
          <div class="w-12 h-12 bg-gradient-to-r from-blue-600 to-purple-600 rounded-xl flex items-center justify-center shadow-lg floating-animation group-hover:scale-105 transition-transform">
            <span class="text-white font-bold text-xl">R</span>
          </div>
          <div class="text-left">
            <div class="text-2xl font-bold text-gray-900">ReZum</div>
            <div class="text-sm text-gray-500">AI-Powered Career Tools</div>
          </div>
        <% end %>
      </div>

      <!-- Page Title -->
      <div class="mt-8 text-center fade-in-up">
        <h2 class="text-3xl font-bold text-gray-900">
          <%= yield(:page_title) %>
        </h2>
        <% if content_for?(:page_subtitle) %>
          <p class="mt-2 text-sm text-gray-600">
            <%= yield(:page_subtitle) %>
          </p>
        <% end %>
      </div>
    </div>

    <!-- Main Form Container -->
    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-2xl fade-in-up">
      <div class="glassmorphism rounded-2xl shadow-xl px-8 py-8 max-h-[80vh] overflow-y-auto">
        <!-- Flash Messages -->
        <% if notice %>
          <div class="mb-4" data-controller="alert" data-alert-timeout-value="10000">
            <div class="bg-white border border-green-200 text-gray-800 px-4 py-3 rounded-2xl shadow-lg flex items-start gap-3">
              <div class="flex-shrink-0 w-10 h-10 bg-gradient-to-br from-green-400 to-green-600 rounded-full flex items-center justify-center shadow-md">
                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
              </div>
              <div class="flex-1 pt-1">
                <p class="text-sm font-medium text-gray-900">Success!</p>
                <p class="text-sm text-gray-600 mt-0.5"><%= notice %></p>
              </div>
              <button type="button" data-action="alert#dismiss" class="flex-shrink-0 text-gray-400 hover:text-gray-600 transition-colors p-1">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
              </button>
            </div>
          </div>
        <% end %>
        
        <% if alert %>
          <div class="mb-4" data-controller="alert" data-alert-timeout-value="10000">
            <div class="bg-white border border-red-200 text-gray-800 px-4 py-3 rounded-2xl shadow-lg flex items-start gap-3">
              <div class="flex-shrink-0 w-10 h-10 bg-gradient-to-br from-red-400 to-red-600 rounded-full flex items-center justify-center shadow-md">
                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                </svg>
              </div>
              <div class="flex-1 pt-1">
                <p class="text-sm font-medium text-gray-900">Oops!</p>
                <p class="text-sm text-gray-600 mt-0.5"><%= alert %></p>
              </div>
              <button type="button" data-action="alert#dismiss" class="flex-shrink-0 text-gray-400 hover:text-gray-600 transition-colors p-1">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
              </button>
            </div>
          </div>
        <% end %>

        <!-- Main Content -->
        <%= yield %>
      </div>

      <!-- Footer Links -->
      <div class="mt-6">
        <%= yield(:footer_links) %>
      </div>
    </div>

    <!-- Global Features -->
    <div class="mt-8 text-center fade-in-up">
      <div class="flex justify-center items-center space-x-6 text-sm text-gray-500">
        <div class="flex items-center space-x-1">
          <span>🌍</span>
          <span>15+ Countries</span>
        </div>
        <div class="flex items-center space-x-1">
          <span>🔒</span>
          <span>GDPR Compliant</span>
        </div>
        <div class="flex items-center space-x-1">
          <span>⚡</span>
          <span>30s Generation</span>
        </div>
      </div>
    </div>

    <!-- Background Decorations -->
    <div class="fixed inset-0 -z-10 overflow-hidden">
      <div class="absolute -top-40 -right-32 w-80 h-80 bg-purple-300 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-animation"></div>
      <div class="absolute -bottom-40 -left-32 w-80 h-80 bg-blue-300 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-animation" style="animation-delay: -3s;"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-80 h-80 bg-yellow-300 rounded-full mix-blend-multiply filter blur-xl opacity-10 floating-animation" style="animation-delay: -1.5s;"></div>
    </div>
  </div>

  <!-- Country Detection for New Users -->
  <div id="country-detection" 
       data-controller="country-detection" 
       data-country-detection-api-url="/api/detect-country"
       class="hidden"></div>
</body>
</html>