nigeria:
  code: NG
  name: Nigeria
  currency: NGN
  timezone: Africa/Lagos
  supported_languages: <%= ["en"].to_json %>
  payment_methods: <%= ["paystack"].to_json %>
  vat_rate: 0.075
  is_active: true

united_states:
  code: US
  name: United States
  currency: USD
  timezone: America/New_York
  supported_languages: <%= ["en", "es"].to_json %>
  payment_methods: <%= ["paystack"].to_json %>
  vat_rate: 0.0
  is_active: true
